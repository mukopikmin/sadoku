# Local Setup

This document is a second-level nested fixture for directory preview mode.

## Verification

- Start Sadoku with `test/integration/fixtures` as the document directory.
- Select `guides/setup/local.md` from the document list.
- Confirm that this content is rendered.
