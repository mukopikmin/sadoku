interface:
  display_name: "Release Sadoku"
  short_description: "Propose and safely publish a Sadoku release"
  default_prompt: "Use $sadoku-release to propose the next semantic version and safely prepare and publish the release."
